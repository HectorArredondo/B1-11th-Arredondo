<%= form_for(reciè) do |f| %>
  <% if reciè.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reciè.errors.count, "error") %> prohibited this reciè from being saved:</h2>

      <ul>
      <% reciè.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :ingredients %>
    <%= f.text_area :ingredients %>
  </div>

  <div class="field">
    <%= f.label :preparation %>
    <%= f.text_area :preparation %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
